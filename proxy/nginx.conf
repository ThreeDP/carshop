server {
    listen      8000;
    server_name locahost;

    location /v1/assets/images {
        root /images;
        autoindex on;
    }

    location / {
        proxy_pass          http://frontend:5061;
        proxy_http_version  1.1;
    }
}